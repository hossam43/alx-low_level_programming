#include <stdio.h>
/* more headers goes there */
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
int ch, kh;
for (ch = '0'; ch <= '9'; ch++)
{
	for (kh = '0'; kh <= '9'; kh++)
	{
	putchar(ch);
	putchar(kh);
	putchar(',');
	putchar(' ');
	}
}
putchar('\n');
return (0);
}
